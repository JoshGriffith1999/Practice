#include"myHeader.h"

int main( int argc, char* argv[]){

    char passString[] = "la la la,la lalalalalalalalalalalalalalalalal hello world\n";

    enum cattleQualityGrade currentCow[5] = { PRIME,CHOICE, CUTTER, CANNER, UTILITY };

    //printSomething( passString );

    //for(int i = 0; i < 5; i++)
    //    cattleDetermination( currentCow[i] );

    //doesFileExist( "topSecret.txts" );

    //printTree( atoi(argv[1]) );

    //int logError = fLogMsg( "logmessage.log.241227", "LOG this is a log message" );

    fMemory2( 5 );

	return 0;
}
